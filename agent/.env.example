# Copy to .env (never commit .env) and add your free key from https://console.groq.com/keys
GROQ_API_KEY=
# Optional: choose another Groq model that supports tool calling
# AGENT_MODEL=openai/gpt-oss-120b
